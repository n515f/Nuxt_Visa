{
  "compilerOptions": {
    /* ==============================
       📦 الأساسيات
    ============================== */
    "target": "ES2022",                      // تحديث لدعم أحدث مميزات JS بثبات
    "lib": ["DOM", "DOM.Iterable", "ES2022"], // مكتبات ضرورية لـ React والـ DOM
    "module": "ESNext",                      // أفضل توافق مع Vite
    "moduleResolution": "bundler",           // يستخدم نظام التحزيم الحديث (Vite)
    "jsx": "react-jsx",                      // لتحسين أداء JSX
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "noEmit": true,                          // Vite يتولى التحزيم، لا نحتاج إخراج ملفات JS
    "skipLibCheck": true,                    // لتسريع الترجمة

    /* ==============================
       ✅ الصرامة والأنواع
    ============================== */
    "strict": true,                          // تفعيل نمط صارم يقلل الأخطاء المنطقية
    "noImplicitAny": true,                   // يمنع المتغيرات بدون نوع محدد
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,

    /* ==============================
       🧭 المسارات والمسميات المستعارة
    ============================== */
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    },

    /* ==============================
       ✨ تحسينات إضافية
    ============================== */
    "forceConsistentCasingInFileNames": true, // يمنع أخطاء التسمية بين أنظمة التشغيل
    "useDefineForClassFields": true,          // مطلوب لبعض مواصفات JS الحديثة
    "moduleDetection": "force"                // يجبر TypeScript على اعتبار كل ملف كموديل
  },

  /* ==============================
     🧩 تضمين المجلدات
  ============================== */
  "include": ["src"]
}
